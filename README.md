# نظام إدارة الحضور والغياب والإجازات والرواتب

نظام شامل لإدارة حضور الموظفين، غيابهم، إجازاتهم، ورواتبهم مع دعم Firebase.

## 📋 المميزات

- ✅ إدارة الموظفين وجداول الدوام المرنة
- ✅ نظام حضور وغياب متقدم
- ✅ إدارة الإجازات بأنواعها المختلفة
- ✅ أرصدة إجازات مستقلة لكل نوع (بالدقائق)
- ✅ نظام إعفاءات مرن
- ✅ قواعد خصم قابلة للتخصيص
- ✅ حساب الرواتب بدورات مرنة
- ✅ تقارير شاملة
- ✅ لوحة تحكم كاملة (Frontend)

## 🏗️ هيكل المشروع

```
dawam_pro/
├── src/                    # Backend (Node.js + Express)
│   ├── config/            # إعدادات Firebase
│   ├── types/             # أنواع البيانات
│   ├── lib/               # مكتبات مساعدة
│   ├── repositories/       # مستودعات البيانات
│   ├── services/          # خدمات الأعمال
│   ├── controllers/       # المتحكمات
│   ├── routes/            # المسارات
│   └── server.ts          # نقطة البداية
├── frontend/              # Frontend (Next.js)
│   ├── src/
│   │   ├── app/          # صفحات Next.js
│   │   ├── components/   # مكونات React
│   │   └── lib/          # مكتبات مساعدة
│   └── package.json
└── README.md
```

## 🚀 البدء السريع

### 1. إعداد Backend

```bash
# تثبيت التبعيات
npm install

# إعداد Firebase (راجع SETUP.md)
# إنشاء ملف .env مع بيانات Firebase

# تشغيل Backend
npm run dev
```

### 2. إعداد Frontend

```bash
cd frontend

# تثبيت التبعيات
npm install

# إعداد ملف .env.local
cp .env.local.example .env.local

# تشغيل Frontend
npm run dev
```

### 3. الوصول للتطبيق

- **Backend API**: http://localhost:3000
- **Frontend Dashboard**: http://localhost:3001 (أو المنفذ الافتراضي لـ Next.js)

## 📚 التوثيق

- [دليل الإعداد السريع](./SETUP.md)
- [دليل Frontend](./frontend/README.md)
- [دليل النشر على GitHub و Vercel](./DEPLOY_GUIDE_AR.md) ⭐
- [نشر سريع](./QUICK_DEPLOY.md)

## 🔥 إعدادات Firebase

### Backend (Admin SDK)

المشروع يستخدم Firebase Admin SDK للعمليات الخلفية. الإعدادات موجودة في `src/config/firebase.ts`.

### Frontend (Client SDK)

إعدادات Firebase Client SDK موجودة في `src/config/firebase-client.ts` للاستخدام في الواجهة الأمامية.

## 📡 API Endpoints

### الموظفون
- `GET /api/v1/employees` - قائمة الموظفين
- `POST /api/v1/employees` - إضافة موظف
- `GET /api/v1/employees/:id` - تفاصيل موظف
- `PUT /api/v1/employees/:id` - تحديث موظف
- `DELETE /api/v1/employees/:id` - حذف موظف

### الحضور
- `POST /api/v1/attendance` - تسجيل حضور
- `GET /api/v1/attendance/employee/:id/date/:date` - حضور موظف في تاريخ
- `GET /api/v1/attendance/evaluate/:id/:date` - تقييم يوم

### الرواتب
- `GET /api/v1/payroll/calculate/:id` - حساب الراتب
- `GET /api/v1/payroll/cycle/:id` - حساب الراتب حسب الدورة

## 🛠️ التطوير

### Backend

```bash
npm run dev      # وضع التطوير
npm run build    # بناء المشروع
npm start        # تشغيل الإنتاج
```

### Frontend

```bash
cd frontend
npm run dev      # وضع التطوير
npm run build    # بناء المشروع
npm start        # تشغيل الإنتاج
```

## 📝 الملاحظات

- جميع التواريخ تُحفظ بصيغة ISO string
- جميع الأوقات تُحسب بالدقائق
- النظام يدعم نماذج بيانات زمنية (Temporal Data Model)
- لوحة التحكم تدعم اللغة العربية بالكامل

## 📄 الترخيص

ISC
