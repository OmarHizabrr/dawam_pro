# 🚀 دليل النشر خطوة بخطوة

## 📦 الجزء الأول: رفع المشروع على GitHub

### ✅ الخطوة 1: إنشاء مستودع GitHub

1. افتح [github.com](https://github.com) وسجّل الدخول
2. انقر **"+"** في الزاوية اليمنى → **"New repository"**
3. املأ:
   - **Name**: `dawam-pro`
   - **Description**: نظام إدارة حضور وغياب وإجازات ورواتب
   - اختر **Public** أو **Private**
4. **⚠️ مهم:** لا تضع علامة على أي خيار (README, .gitignore, license)
5. انقر **"Create repository"**

### ✅ الخطوة 2: إنشاء Personal Access Token

1. في GitHub: صورتك الشخصية → **Settings**
2. **Developer settings** → **Personal access tokens** → **Tokens (classic)**
3. **Generate new token (classic)**
4. املأ:
   - **Note**: `dawam-pro-deploy`
   - **Expiration**: اختر المدة
   - **Select scopes**: ✅ **repo** (كل الصلاحيات)
5. **Generate token**
6. **⚠️ انسخ الـ Token فوراً!** (سيظهر مرة واحدة فقط)

### ✅ الخطوة 3: رفع المشروع

افتح **Command Prompt** في مجلد المشروع:

```cmd
git init
git add .
git commit -m "Initial commit: نظام إدارة الحضور والرواتب"
git remote add origin https://github.com/YOUR_USERNAME/dawam-pro.git
git branch -M main
git push -u origin main
```

**عند الطلب:**
- **Username**: اسم المستخدم في GitHub
- **Password**: الـ Token الذي نسخته (ليس كلمة المرور!)

---

## 🌐 الجزء الثاني: نشر المشروع على Vercel

### ✅ الخطوة 1: إنشاء حساب Vercel

1. افتح [vercel.com](https://vercel.com)
2. **Sign Up** → **Continue with GitHub**
3. سجّل الدخول وامنح الصلاحيات

### ✅ الخطوة 2: نشر Backend

1. **Add New Project** → اختر `dawam-pro` → **Import**

2. **إعدادات:**
   ```
   Framework Preset: Other
   Root Directory: . (فارغ)
   Build Command: npm run build:backend:vercel
   Output Directory: dist
   Install Command: npm install
   ```

3. **Environment Variables** (أضف كل متغير):
   ```
   FIREBASE_PROJECT_ID = dawam-alhikma
   FIREBASE_CLIENT_EMAIL = your-service-account@dawam-alhikma.iam.gserviceaccount.com
   FIREBASE_PRIVATE_KEY = "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
   PORT = 3000
   NODE_ENV = production
   FRONTEND_URL = https://your-frontend.vercel.app (سنضيفه لاحقاً)
   ```
   - اختر: **Production, Preview, Development**

4. **Deploy** → انتظر → **انسخ الرابط** (مثل: `https://dawam-pro-backend.vercel.app`)

### ✅ الخطوة 3: نشر Frontend

1. **Add New Project** → اختر نفس المستودع `dawam-pro` → **Import**

2. **إعدادات:**
   ```
   Framework Preset: Next.js
   Root Directory: frontend
   (الباقي تلقائي)
   ```

3. **Environment Variables:**
   ```
   NEXT_PUBLIC_API_URL = https://your-backend.vercel.app
   ```
   (ضع رابط Backend من الخطوة السابقة)

4. **Deploy** → انتظر → **انسخ الرابط** (مثل: `https://dawam-pro-frontend.vercel.app`)

### ✅ الخطوة 4: ربط Backend و Frontend

1. **Backend:**
   - Settings → Environment Variables
   - عدّل `FRONTEND_URL` = رابط Frontend
   - **Redeploy**

2. **Frontend:**
   - تأكد من أن `NEXT_PUBLIC_API_URL` = رابط Backend الصحيح

---

## ✅ التحقق

### Backend:
افتح: `https://your-backend.vercel.app/health`
يجب أن ترى: `{"status":"ok","timestamp":"..."}`

### Frontend:
افتح: `https://your-frontend.vercel.app`
يجب أن ترى لوحة التحكم

---

## 🔄 التحديثات المستقبلية

```cmd
git add .
git commit -m "وصف التغييرات"
git push
```

Vercel سينشر التحديثات تلقائياً! 🎉

---

## 📖 ملفات مساعدة

- `DEPLOY_GUIDE_AR.md` - دليل تفصيلي كامل
- `HOW_TO_CREATE_GITHUB_TOKEN.md` - كيفية إنشاء Token
- `VERCEL_SETTINGS.md` - إعدادات Vercel المفصلة

---

## 🎉 مبروك!

الآن لديك:
- ✅ مشروع على GitHub
- ✅ Backend يعمل على Vercel
- ✅ Frontend يعمل على Vercel
- ✅ نظام كامل متاح على الإنترنت!

